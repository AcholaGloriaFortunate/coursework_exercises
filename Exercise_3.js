// Create an object called person with name = John, age = 50. Then
  // access the object to alert("John is 50").

  const person = {
    name: "john",
    age: 50
  };
  
  // Access the properties of the person object and trigger an alert
  console.log(person.name + " is " + person.age);